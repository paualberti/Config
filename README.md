# Config

```Bash
config_dir="$HOME/Config/"
git clone https://github.com/paualberti/Config "$config_dir"
cd "$config_dir"

chmod +x "$config_dir/setup.sh"
./"$config_dir/setup.sh"

chmod +x "$config_dir/symlink.sh"
./"$config_dir/symlink.sh"
```
